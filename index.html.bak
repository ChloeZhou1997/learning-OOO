<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Object-Oriented Thought Process</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@700&family=Lato:wght@400;700&family=Source+Code+Pro:wght@400;600&display=swap" rel="stylesheet">
    
    <script src="https://cdn.tailwindcss.com?plugins=typography"></script>
    <script src="https://unpkg.com/lucide@latest"></script>

    <link rel="stylesheet" href="style.css">
</head>
<body class="bg-white font-lato text-gray-800">

    <div id="app-container" class="relative">
        <!-- Mobile Menu Button -->
        <button id="mobile-menu-button" class="md:hidden fixed top-4 left-4 z-50 p-2 bg-primary text-secondary rounded-full shadow-lg">
            <i data-lucide="menu"></i>
        </button>

        <!-- Navigation Sidebar -->
        <aside id="sidebar" class="bg-primary text-slate-text p-6 flex flex-col transition-all duration-300 ease-in-out">
            <div class="sidebar-header flex justify-between items-center mb-10">
                <h1 class="text-xl font-inter font-bold text-white whitespace-nowrap">The OO Thought Process</h1>
                <button id="sidebar-toggle" class="text-secondary hover:text-white transition-colors">
                    <i data-lucide="chevron-left" class="toggle-icon"></i>
                </button>
            </div>
            
            <nav id="chapter-nav">
                <ul class="space-y-2">
                    <!-- Nav links will be populated here -->
                </ul>
            </nav>
        </aside>

        <!-- Main Content Area -->
        <main id="main-content" class="overflow-y-auto">
            <div class="max-w-4xl mx-auto p-8 pt-20 md:p-12">
                <header class="mb-12">
                     <h1 class="text-5xl font-inter font-bold text-primary mb-2">The Object-Oriented Thought Process</h1>
                     <p class="text-xl text-gray-600">An Interactive Learning Experience</p>
                </header>

                <div id="content-sections" class="prose max-w-none prose-h2:font-inter prose-h2:text-primary prose-h2:mb-4 prose-p:text-gray-700">
                    <!-- Content sections will be populated here -->
                </div>
            </div>
        </main>

        <!-- Mobile Menu Overlay -->
        <div id="mobile-menu-overlay" class="fixed inset-0 bg-black bg-opacity-50 z-30 hidden"></div>
    </div>

    <script type="module" src="script.js"></script>
</body>
</html>
